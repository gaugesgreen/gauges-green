# Agents

The v0.1 repo ships one synthetic agent under `agents/demo`.

Agent directories contain:

- `SOUL.md` for identity
- `AGENTS.md` for operating rules
- `TOOLS.md` for available tool contracts
- `boot-manifest.json` for context loading
- `scorecard.md` for human-readable evaluation evidence
