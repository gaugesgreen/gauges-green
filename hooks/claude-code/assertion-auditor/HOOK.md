# Assertion Auditor Hook

Example hook contract:

```bash
python3 hooks/claude-code/assertion-auditor/handler.py path/to/output.md
```

The handler exits non-zero when unsupported high-confidence claims are found.
