# Demo Tool Contracts

Available public demo tools:

- `boot`: compile the agent boot payload from the manifest.
- `gates`: check generated text for unsupported assertions, public positioning
  drift, date arithmetic errors, and recipient-language issues.
- `scorecard`: emit the synthetic scorecard row.

The demo has no network, mailbox, calendar, CRM, or messaging credentials.
